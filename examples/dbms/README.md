# dbms example

This directory contains an ISL implementation of a small RDBMS.

- Fixed MVP specification: `examples/dbms/SPEC.md`
- Minimum SQL scope: `CREATE TABLE`, `INSERT`, `SELECT`, `UPDATE`, `DELETE`

Implementation must follow the fixed spec before adding extra SQL features.
