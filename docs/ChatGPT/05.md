# 5. リストとシーケンス

## 基本操作
```lisp
(car '(a b c))
(cdr '(a b c))
(cons 'z '(a b c))
```

## 変換・探索
```lisp
(mapcar (lambda (x) (+ x 1)) '(1 2 3))
(find 3 '(1 2 3 4))
(remove-if oddp '(1 2 3 4 5))
```

## 集約
```lisp
(reduce + '(1 2 3 4))
```

## 配列/ベクタ
- 順次処理中心ならリスト
- ランダムアクセス中心ならベクタ/配列
