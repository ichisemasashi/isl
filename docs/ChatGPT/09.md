# 9. 条件処理とデバッグ

## 非局所制御
```lisp
(catch 'done
  (throw 'done 42)) ; => 42
```

## 条件処理
`handler-case` を持つ処理系では、条件タグごとに分岐できます。

```lisp
(handler-case
  (/ 1 0)
  (error (e) 'failed))
```

## デバッグ補助
`trace` / `untrace` / `break` / `debug` 等は処理系拡張であることが多いです。
