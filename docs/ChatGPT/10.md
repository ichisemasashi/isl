# 10. マクロとメタプログラミング

## マクロ定義
```lisp
(defmacro when (test &rest body)
  `(if ,test (progn ,@body) nil))
```

## 展開確認
```lisp
(macroexpand '(when t (print 1)))
```

## 原則
- マクロは「構文」を作る
- 関数は「値」を計算する
- 副作用や評価回数に注意して設計する
