# 11. プログラム構成（標準/拡張の境界）

## 標準で安全な構成
- ファイル分割
- `load` による読込
- 関数/クラス/マクロの責務分離

## 実装拡張としてよくある機能
- `require` / `provide`
- パッケージシステム（`defpackage`/`in-package`）
- モジュール管理ユーティリティ

これらは **処理系ごとに互換性がない** ため、移植性が必要なら標準機能中心で設計する。
