# **ISLISP: A Practical Guide**

## **第1部: ISLISP入門**
1. **ISLISP入門**
  - ISLISPとは？
  - ISLISP環境の設定（https://github.com/sasagawa888/eisl）
  - 初めてのISLISPプログラムの実行

2. **基本構文とデータ型**
  - アトム、シンボル、数値
  - リスト、consセル、配列
  - 文字列と文字
  - 変数と代入

3. **制御構造と関数**
  - 条件分岐：`if`、`cond`、`case`
  - ループ：`loop`、`while`、`do`
  - `defun` による関数の定義
  - 再帰
  - `progn` および `block` 構文
  - `return-from` および `go` の使用

---

## **第2部：ISLISPプログラミングの基礎**
4. **関数と高階プログラミング**
  - 関数の定義と使用
  - ラムダ関数
  - 関数を引数として渡す

5. **リストとシーケンスの操作**
  - 基本的なリスト操作 (`car`, `cdr`, `cons`)
  - マッピングとフィルタリング (`mapcar`, `remove-if`)
  - `reduce` と再帰の使用

6. **シンボル、ネームスペース、スコープ**
  - グローバル変数とローカル変数
  - `let`、`let*`、動的スコープ
  - パッケージの作成と使用

7. **ISLISPにおけるオブジェクト指向プログラミング**
  - ISLISPのオブジェクトシステム入門
  - `defclass`によるクラスの定義
  - オブジェクトの作成と使用
  - `defmethod`によるメソッドと継承(inheritance)
  - オーバーライド
  - ジェネリック関数による多重ディスパッチ

8. **ファイル入出力とシステムとのやりとり**
  - ファイルの読み書き（`with-open-file`）
  - ファイルへのデータの追加
  - ファイルの確認と削除（`probe-file`, `delete-file`）
  - ストリームと出力の表示
  - `format` によるテキストの書式設定
  - シェルコマンドの実行（`system`）
  - 環境変数の取得 (`getenv`)

---

## **第3部：高度なトピック**
9. **ISLISPの高度な概念**
  - エラー処理のための `catch` と `throw` の使用
  - `debug`、`trace`、`break` によるデバッグ
  - `handler-case` による例外処理
  - ハッシュテーブルとベクトルの使用
  - 末尾再帰の最適化

10. **マクロとメタプログラミング**
  - ISLISPにおけるマクロの理解
  - `defmacro` によるカスタムマクロの作成
  - コード展開（`macroexpand`）の理解
  - カスタム制御構造の作成
  - コード簡略化のためのマクロ
  - マクロのデバッグテクニック

11. **モジュールとコードの構成**
  - モジュールの作成と使用
  - `require` および `provide` システム
  - 再利用可能なコードの記述

12. **外部システムとのインターフェイス**
  - ISLISP からの C 関数の呼び出し
  - Common Lisp との相互運用
  - システム・プロセスの操作
  - データベースおよびネットワークとのインターフェイス
  - 外部ライブラリの使用
  - 外部ライブラリの記述

---

## **第4部：実世界のISLISPアプリケーション**
13. **ISLISPでの小規模アプリケーションの構築**
  - シンプルなコマンドラインツールの設計
  - ユーザー入力の解析と処理
  - モジュール化および再利用可能なコードの記述

14. **パフォーマンスの最適化とベストプラクティス**
  - ISLISPコードのプロファイリング
  - ガーベッジコレクションの理解
  - 再帰関数の最適化
  - 効率的なリストおよび配列処理
  - メモリ管理のヒント
  - 慣用的で保守しやすいISLISPコードの記述

15. **ISLISPアプリケーションの配備と配布**
  - ISLISPプログラムのパッケージ化
  - 運用環境でのISLISPアプリケーションの実行
  - ISLISP配備のベストプラクティス
