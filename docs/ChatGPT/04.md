# 4. 関数と高階プログラミング

## 関数
```lisp
(defun add (x y) (+ x y))
(add 3 4) ; => 7
```

## ラムダ
```lisp
((lambda (x) (+ x 2)) 3) ; => 5
```

## 高階関数
```lisp
(mapcar (lambda (x) (* x x)) '(1 2 3)) ; => (1 4 9)
(reduce + '(1 2 3 4)) ; => 10
```

## 注意
`#'` 記法や Common Lisp 固有補助関数は、処理系により未対応の場合があります。
